rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /bakeries/{bakeryId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.bakeryId == bakeryId;
    }
  }
}
